namespace Grafana {
  export class GrafanaUtils {
    static updateTimeRangeFromGrafanaUrl(baseUrl: string,
                                         from: number,
                                         to: number = Date.now()): string {
      // baseUrl.replace(/)
      return baseUrl;
    }
  }
}
